# ROADMAP — youtube-feed 앞으로의 방향

> 원칙: **필요한 것만 그때그때.** 아프기 전엔 도입하지 않는다.
> 지금 상태(2026-08-02): 정적 사이트(gh-pages). 화면이 열릴 때 **내 Cloudflare Worker**로 채널 RSS를,
> **구글 OAuth**로 내 구독·재생목록을 직접 읽는다. 내 데이터는 `localStorage`에 있고 드라이브로 올려 기기끼리 잇는다.
> (`collect.py`가 HTML까지 찍던 초기 구조는 2026-07-28에 중단, 07-31에 삭제 — 아래 기록은 그 시절 기준으로 읽을 것)

---

## 0. 공통 토대 (두 갈래 모두를 쉽게 만드는 한 수)

핵심을 한 문장으로: **파이썬과 화면 사이에 "계약(interface)"을 하나 두고, 그 계약 뒤에서 서로를 모르게 만든다.**

**진행 상황 — 계약 v0 (완료, 2026-07-26)**
`template.html`이 `{{TITLE}}`·`{{UPDATED}}`·`{{BODY}}` 토큰을 선언하고, collect.py가 `.replace()`로 채운다.
= "화면이 요구하는 것"의 목록이 코드 밖으로 처음 나온 상태. 다만 계약 단위가 **문자열 조각**이라 거칠다
(특히 `{{BODY}}` 안에 `<h2>/<p>/<a>` 화면 구조를 아직 파이썬이 쥐고 있음).

**계약 v1 — 데이터의 "모양"을 인터페이스로 (완료, 2026-07-28)**
계약을 문자열 조각이 아니라 **데이터 구조(JSON 스키마)**로 올렸다. 화면이 받아야 할 것을 모양으로 선언:

```json
{ "updated": "...", "sections": [
    { "topic": "체스", "name": "체스프릭",
      "videos": [ { "title": "...", "link": "...", "date": "..." } ] } ] }
```

- ✅ collect.py → 이 모양의 `data.json`만 뱉음 (HTML 태그를 하나도 모름). `build_html` 제거.
- ✅ 화면 → `index.html`(정적 껍데기) + `app.js`가 `fetch("data.json")` 후 DOM 렌더 (파이썬을 하나도 모름).
- ✅ `template.html`은 역할이 index.html로 흡수되어 삭제.
- ✅ 둘 사이 의존성이 **JSON 파일 하나로 완전히 끊김.** push→Actions→gh-pages→라이브까지 검증(1f13e40).
- 배운 것: fetch, DOM 렌더(createElement/textContent, XSS 안전), 데이터/뷰 분리.
- 주의: `data.json`은 생성물이지만 `index.html`처럼 **추적**한다. `.gitignore`하면 gh-pages 게시에서 빠져 404(2026-07-24 교훈).

**왜 이게 값진가 — 계약을 밖으로 꺼낸 진짜 보상**
- FE는 데이터 걱정 없이 두꺼워질 수 있다 (갈래 1).
- 개인화는 json 자리에 "사용자별 데이터"만 끼우면 된다 (갈래 2).
- **수집기를 무엇으로 만들든 화면은 무풍지대.** 파이썬을 Go로 바꾸든, 서버가 실시간으로 만들어주든,
  더 유연한 미들웨어를 계약 뒤에 숨기든 — 계약(JSON 모양)만 지키면 화면 코드는 한 줄도 안 바뀐다.
  즉 언어·미들웨어 교체가 이 지점부터 **공짜**가 된다.

> 신호등: 계약 v0(문자열 토큰)로 화면 작업이 답답해지는 순간
> (예: "같은 데이터를 다르게 보여주고 싶다", "화면에서 필터/정렬하고 싶다")이 v1로 갈 때다.
> → v1 도달 완료. 다음 신호등: 화면에서 필터·정렬·검색이 필요해지는 순간이 갈래1·2단계 확장 때다.

---

## 갈래 1 — FE가 두꺼워질 때

**1단계 · 템플릿 분리 (완료, 2026-07-26 → 이후 계약 v1로 흡수돼 `template.html`은 삭제)**
HTML/CSS를 Python 문자열에서 빼내 `template.html` + `style.css`로.
collect.py는 데이터만 채운다 → "화면 고칠 때 파이썬 안 건드림".
- 배우는 것: 관심사 분리, 문자열 템플릿(str.format 또는 Jinja2)

**2단계 · 데이터=JSON, 화면=정적 JS (완료, 2026-07-28 — 계약 v1) (핵심 분기)**
collect.py → `data.json`, `index.html` + `app.js`가 fetch해서 렌더.
여기서부터 필터·정렬·검색을 화면 쪽에서 자유롭게 붙인다. GitHub Pages 그대로 OK.
- 배우는 것: fetch, DOM 렌더, 데이터/뷰 분리

**2.5단계 · 화면 분리 (해시 라우팅) — 🚧 뼈대 완료 (2026-08-07), 내용물은 진행 중**

> ✅ **기능 정리(2026-08-07)** — 화면을 나누고 나니 남는 게 눈에 띄었다. **파일 내보내기/가져오기**는
> 드라이브로 갈음해 지웠고(`exportAll`/`importAll`은 그대로 — 저장 위치만 다르다),
> **"나중에 볼에서 뽑은 ID 가져오기"**도 지워 풀을 채우는 길을 재생목록 동기화 하나로 좁혔다.
> `videos.list`를 부르던 코드가 통째로 죽어 사라졌다.
> ✅ **라우팅 뼈대** — 화면 넷·탭·해시 라우팅이 동작한다. 새로고침·뒤로가기 확인.
> 예상대로 재작성이 아니라 **끝의 초기화 6줄을 라우터로 바꾸는 수준**으로 끝났다.
> 배선 코드는 한 줄도 안 고쳤다 — `wire*`가 전부 `getElementById` + 가드로 시작해서
> **요소를 지우지 않고 숨기기만** 하면 어느 화면에 있든 그대로 동작한다.
> 덤: 비싼 초기화(피드 렌더 = 채널 수만큼 Worker 요청)를 **피드에 들어올 때로 미뤘다.**
> ✅ **①의 선별 (2026-08-15, `e3931dd`)** — 채널 관리 화면에 **영상 없는 한 줄짜리 목록**을 만들고
> `active` 토글·전부 끄기/켜기를 붙였다. 목록이 피드에 영상과 섞여 있어서(채널당 6개)
> **수백 개를 훑는 것 자체가 불가능했던 것**이 진짜 장애물이었다.
> 구독 가져오기는 이제 `active: false`로 담는다 — `구독 = 내 피드` 전제가 코드에서 사라졌다.
> ✅ **①의 선별, 실제로 끝냈다 (2026-08-20)** — 도구가 아니라 **쓰는 일** 쪽이 남아 있던 것인데 그것도 닫혔다.
> ⬜ **남은 것**: ②의 「더보기」, 죽은 채널 정리(→ RSS 에러 계약이 먼저)

2단계와 React 사이가 비어 있었다. 기능이 다섯을 넘기고 **구독 319개가 들어오면서**
"한 화면에 전부"가 감당이 안 됐는데, 이걸 풀자고 React를 꺼내는 건 과하다.

화면 넷으로 나눈다: **① 채널 관리 ② 메인 피드 ③ 랜덤 추천 ④ 설정** (`#/channels` 식 해시 라우팅)
- `app.js`가 이미 렌더 함수로 나뉘어 있어 **"전부 초기화"를 "하나만"으로 바꾸는 수준**이다
- 정적 사이트라 `pushState`는 새로고침 시 404가 난다 → **해시는 서버 설정 없이 그냥 된다**
- ⭐ **①의 이름이 "추가"가 아니라 "관리"인 게 핵심**이다 — `구독 = 내 피드`라는 전제를 버리고
  **구독은 후보, `myChannels`는 고른 것**으로 가른다. 선별 화면이 곧 319개의 마이그레이션이다
- ⭐ **컬러 변수화를 이것보다 먼저** 한다. 색이 22군데 하드코딩돼 있는데,
  화면이 넷으로 늘어난 뒤에 정리하면 80군데가 된다
- 설계 상세(화면별 책임·`active` 플래그·쿼터 제약·디자인 시스템): `_life`의
  `docs/core/youtube-feed/40-screens-and-ui.md`

**3단계 · 프레임워크 (진짜 앱 될 때만)**
컴포넌트·상태·라우팅이 손으로 벅차지면 그때 React(+Vite).
빌드 결과가 정적 파일이라 Pages/Netlify에 그대로 배포. 취준·이직 이력서에도 바로 먹히는 스택.
- ⚠️ 진짜 아플 때만. 지금 넣으면 오버엔지니어링.
- 화면 넷을 나누는 것만으로는 그 지점이 아니다 → 2.5단계로 간다

**신호등**
- 화면 고치기 귀찮다 → 1단계
- "같은 데이터 다르게 보여주고 싶다" → 2단계
- **한 화면에 다 못 담겠다 / 목록이 사람이 훑을 수 없는 크기다** → 2.5단계 ✅ 켜짐(2026-08-05)
- `if`로 화면 조립이 스파게티 → 3단계

---

## 갈래 2 — 개인화 (로그인·개인 페이지)

순수 정적 사이트(GH Pages)는 로그인·사용자별 저장을 못 한다. 두 국면으로 나눠 접근.

**국면 A · 브라우저에 저장 (완료, 2026-07-28)**
로그인 없이 개인화. 계정·서버 0으로 "내 피드". 한 기기 한정이지만 개인화의 80%를 공짜로.
- ✅ 프리셋 취사선택을 넘어 **사용자가 직접 채널 추가** (링크·@핸들·채널ID 아무거나)
- ✅ `localStorage`에 내 채널 목록 + 결과 캐시(재방문 즉시 렌더 → 백그라운드 갱신)
- ✅ 채널명 자동 추출, 채널별 삭제, 내 채널/추천 채널 구분 렌더
- 배운 것: localStorage, 클라이언트 상태, 캐시-우선 렌더링

**국면 A+ · 릴레이 내재화 (완료, 2026-07-28) — 예정에 없던 필수 우회로**
브라우저에서 유튜브 RSS를 직접 부르면 CORS에 막혀 중간 릴레이가 필요했다.
공개 무료 프록시로 시작했으나 레이트 리밋으로 "3연속 실패"가 잦아 **내 Cloudflare Worker**로 전환.
- ✅ `worker/rss-proxy.js` — @핸들 해결·XML 파싱까지 서버가 처리, `data.json`과 같은 모양의 JSON 반환
- ✅ @핸들 추가 **~20초 → ~1초**, 공개 프록시 전부 제거
- 배운 것: CORS의 실체, 서버리스(Workers), 엣지 캐시, `Vary`와 캐시 정합성
- 유형: L3(요청시 동적·무상태) 내재화 — `_life/service-architecture-types.md`

**국면 A++ · 구글 로그인(OAuth) (완료, 2026-07-31) — 국면 B와 헷갈리기 쉬운 별개의 물건**
`src/youtube.js`에서 구글 OAuth로 **내 유튜브 데이터를 읽을 인가**를 받는다. 스코프는 `youtube.readonly` 하나.
- 클라이언트 시크릿을 안 쓰는 **브라우저 토큰 플로우** → 백엔드 없이 정적 사이트에서 그대로 동작
- 토큰은 1시간, localStorage에 저장하지 않고 **메모리에만**. 페이지를 열면 조용히 재발급
- 보상: **구독 채널 자동 가져오기**(손으로 채널 추가하던 작업이 사라짐) + 비공개 재생목록 읽기
- ⛔ 로그인해도 **WL은 여전히 못 읽는다** (아래 갈래 3 참고). watchme 우회는 그대로 유지
- 이건 **저장이 아니라 읽기 인가**다 → 데이터는 여전히 localStorage에만 있고, **이것만으로는 기기 동기화가 안 된다.**

> **"로그인"이 두 가지라 헷갈린다.** ① 구글 OAuth(위, 완료) = 남의 데이터를 읽을 권한.
> ② 기기 동기화(아래 국면 B, 미착수) = **내 앱 데이터를 내 계정 저장소에 두는 것**.
> 국면 B가 Drive로 정해지면서 ①의 토큰을 ②가 그대로 물려쓰게 됐다 — 두 번째 로그인은 없다.

**국면 A+++ · 내보내기/가져오기 (2026-08-02 완료 → 2026-08-07 삭제) — 국면 B의 이민 수단**
(원래 "A++"로 적었으나 위에 OAuth 기록이 들어오면서 시간순으로 밀렸다)

> ⛔ **파일 UI는 2026-08-07에 지웠다.** 드라이브 동기화가 자리를 대신했고, 둘 다 두는 값보다
> 화면이 단순한 값이 컸다. **다만 `exportAll()`/`importAll()`은 그대로 살아 있다** —
> 드라이브가 그걸 쓴다. 아래 기록은 그래서 여전히 유효하다(파일 껍데기만 사라진 것).
> 잃은 것: 사람이 열어볼 수 있는 사본. 이제 사본은 드라이브 하나뿐이고 구글 계정에 종속된다.
브라우저 데이터를 지우면 전부 날아가는데 계정이 없어 복구 수단이 없었다. 풀이 516개까지 쌓여 미룰 수 없어졌다.
- ✅ `storage.js`의 `exportAll()`/`importAll()` — 백업 파일 한 장(`channelCache`만 제외)
- ✅ 합치기 규칙은 **기존 것이 이긴다** — 이 기기에서 손본 주제·이름과 먼저 담은 `addedAt`을 파일이 못 덮는다.
  본 영상은 이른 시각을 남긴다 → 두 기기에서 번갈아 가져와도 데이터가 줄지 않는다(합집합)
- ⭐ **이 파일 모양이 그대로 국면 B의 payload다.** 서버에 올릴 것도 받을 것도 이것이고 `파일 ↔ 서버`만 바뀐다

**국면 B · 기기 동기화 (Google Drive `appDataFolder`) — 🎯 다음 (2026-08-04 벤더 재결정)**
여러 기기에서 같은 데이터를 보려면 저장 위치가 브라우저 밖에 있어야 한다.
**이미 붙어 있는 구글 OAuth에 스코프 한 줄(`drive.appdata`)을 더해, 앱 전용 숨은 폴더에 백업 JSON 한 장을 두는 방식.**

🚦 **신호등이 켜졌다 (2026-08-04).** "폰에서 담아둔 걸 노트북에서 못 본다"가 실제로 답답해졌다.
이제 이건 학습용 선택이 아니라 **아파서 하는 일**이다 — 프로젝트 원칙에 맞는 착수 시점.

⚠️ **2026-07-30에 여기 적었던 것 셋은 지금 사실이 아니다.** 그대로 따라가면 안 해도 될 일을 한다:
- ~~호스팅을 Pages 대신 Vercel/Netlify로~~ → **필요 없다.** 비밀키를 쥘 서버가 애초에 필요 없다.
  **제일 무거워 보이던 항목이 애초에 없는 항목이었다.**
- ~~collect.py가 Actions에서 DB에 써넣는 구조~~ → `collect.py`는 2026-07-31에 삭제됐다. 죽은 계획.
- ~~"진짜 로그인이 필요해지면"~~ → 구글 OAuth는 2026-07-31에 이미 붙었다. 신원도 토큰도 손에 있다.

→ 국면 B는 "인증 + 호스팅 + 파이프라인 개조"가 아니라 **"저장 위치 교체" 하나**로 줄었다.

**왜 Drive appData인가 — 2026-08-02의 판단을 뒤집었다**

08-02에는 "Drive는 일이 제일 적지만 **배우는 게 없어서** 안 골랐다"고 적고 Supabase를 택했다.
08-04에 뒤집은 이유: **실제 아픔이 생겼고, 그 아픔을 푸는 데 Supabase는 필요 없는 부품이 너무 많다.**

| 안 | 드는 일 | 판단 (2026-08-04) |
|---|---|---|
| **Google Drive `appDataFolder`** | **제일 적다.** 있는 OAuth에 스코프 한 줄 | ✅ **채택.** 벤더 0개 추가, 서버 0개, 두 번째 로그인 없음 |
| Supabase | 중간 | ❌ SQL·RLS 학습은 매력적이나 **이 문제를 풀려고 벤더·계정·테이블·세션을 새로 들인다.** 학습은 학습용 프로젝트에서 |
| Cloudflare D1/KV + 기존 Worker | 중간 | ❌ 벤더를 안 늘리는 건 같은데 **Worker에 인증·저장 로직을 새로 짜야** 한다 (OAuth 덕에 예전보다 쉬워졌지만 여전히 더 많다) |
| Firebase Firestore | 중간 | ❌ 구글 로그인 재활용·실시간은 좋으나 벤더가 늘고 NoSQL 모델을 새로 익혀야 한다 |
| GitHub Gist(비공개) | 적어 보임 | ❌ PAT를 브라우저에 둬야 하는데 **토큰 스코프가 너무 넓다.** 정적 사이트에서 안전하지 않다 |

⭐ **결정적 이유는 "일이 적다"가 아니라 재사용이다.** 08-02에 만든 백업 파일이 이미
`exportAll()` → JSON 한 장 → `importAll()`(기존 것이 이기는 병합) 모양이다.
Drive는 **그 파일을 그대로 올리고 내리는 것**이라 `파일 ↔ Drive`만 바뀐다.
다른 안들은 데이터를 행·문서로 쪼개야 해서 **이 재사용이 깨진다.**

**잃는 것 (정직하게 남긴다)**
- SQL·DB 스키마·RLS를 이번엔 **안 배운다.** 필요하면 별도 학습 프로젝트로 떼어낸다
- 쿼리가 안 된다. 파일 통째로 읽고 쓴다 → 데이터가 아주 커지면 다시 검토 (지금 규모는 수백 KB)
- 구글 계정에 종속된다. 계정이 잠기면 접근 경로가 함께 잠긴다 → ~~백업 파일은 계속 유지한다~~
  **⚠️ 2026-08-07에 백업 파일을 지웠다. 이 대가를 받아들이기로 한 것이다.**

**⚠️ Drive 방식의 함정 (0단계에서 확인할 것)**
- **토큰이 메모리에 1시간, refresh token이 없다**(정적 사이트의 대가, `youtube.js` 주석 그대로).
  → **동기화는 로그인 세션에 종속된다.** 로그인 안 한 채로 쓰면 로컬만 바뀌고, 다음 로그인 때 밀어올려야 한다
- 스코프 추가 = **동의 화면을 다시 받는다.** Google Cloud Console에서 **Drive API 활성화**도 필요
- `appDataFolder`는 사용자에게 **안 보인다.** 사람이 직접 열어 고칠 수 없다
  (2026-08-07 이후로는 이걸 메워줄 백업 파일이 없다)
- **충돌**: 파일 통째 쓰기라 두 기기가 같은 시간에 쓰면 나중 쓰기가 이긴다.
  → 대응은 이미 있다. **"내려받아 `importAll`로 병합한 뒤 올린다"** — 덮어쓰기가 아니라 합치기

**⭐ 진짜 비용은 벤더가 아니라 `storage.js`가 동기(sync)라는 것** (벤더가 바뀌어도 그대로 남는 문제)

```js
export const loadChannels = () => loadJSON(KEYS.channels, []);  // 배열을 즉시 반환
```

원격으로 가면 전부 `async`가 되고 **`app.js`에서 부르는 모든 곳으로 번진다.**
"`storage.js` 안쪽만 바꾸면 된다"는 계획이 깨지는 유일한 지점이다.
- **후보 1 — 캐시-우선**: localStorage를 버리지 않고 캐시로 남긴다. 읽기는 로컬에서 동기로,
  쓰기는 로컬에 먼저 쓰고 Drive로 밀어올린다. 동기 API가 살고 오프라인에서도 된다.
  `channelCache`로 이미 해본 패턴의 재사용
- **후보 2 — 전면 async**: 정직하지만 변경이 앱 전체로 번진다
- ⏸ **아직 정하지 않는다 (2026-08-04).** 3단계(수동 버튼)까지는 이 결정 없이 갈 수 있다.
  **4단계에 들어갈 때 실제로 부딪혀 보고 정한다.** 이게 국면 B의 실질적 설계 분기점인 건 여전하다

**순서** — 단계마다 쓸 수 있는 물건이 나오게 쪼갠다
1. ✅ 내보내기/가져오기 (2026-08-02) — 이민 수단
2. ✅ **0단계** (2026-08-05) — Drive API 활성화 + 스코프 `drive.appdata`. 로그인 한 번으로 둘 다 받는다
3. ✅ **수동 동기화** (2026-08-05, `3f20d6b`) — `☁️ 올리기` / `☁️ 내려받기`.
   `exportAll`/`importAll` 그대로 재사용했고 **왕복 실동작 확인**. 예고한 대로 **여기까지가 보상의 대부분**이었다.
   올리기는 덮어쓰기가 아니라 **내려받아 합친 뒤 올린다** → 어느 기기에서 눌러도 데이터가 줄지 않는다
   > ⚠️ **2026-08-15에 이 규칙을 통째로 뒤집었다**(`ee2a4c1`). 위 "합치기"는 **더 이상 사실이 아니다** —
   > 아래 3.5를 보라. 이 줄은 그때 왜 합치기를 골랐는지 남기려고 둔다(당시엔 그게 맞았다).
3.5 ✅ **대칭 덮어쓰기로 전환** (2026-08-15, `ee2a4c1`) — 합치기를 버렸다.

   | | |
   |---|---|
   | 올리기 | 내 기기 → 드라이브를 **덮어쓴다** |
   | 내려받기 | 드라이브 → 내 기기를 **덮어쓴다** |

   합치기는 데이터가 절대 줄지 않아 안전했지만, **바로 그래서 아무것도 지울 수 없었다.**
   지운 채널이 클라우드에서 다시 딸려와 되살아났고 그게 저장돼 클라우드에서도 안 없어졌다.
   채널 선별(`active`)이 붙은 뒤로는 **"줄이는 것"이 곧 기능**이라, 줄일 수 없는 동기화는
   안전한 게 아니라 쓸모가 없었다.
   - ⭐ **안전한 쪽이 늘 옳은 게 아니라, 무엇이 기능인지가 정한다.** 08-05엔 합치기가 맞았다 —
     그땐 "담아둔 게 사라지지 않는 것"이 목적이었으니까. 목적이 바뀌자 규칙도 바뀌었다
   - ⭐ **tombstone을 안 만들고 풀었다.** 필드별 시각이라는 정교한 해법을 준비하고 있었는데,
     규칙을 단순하게 바꾸니 문제 자체가 사라졌다
   - ⚠️ 대가: 누르는 쪽이 반대편을 지운다 → **양쪽 다 확인창**(몇 개 사라지는지 숫자로)
   - ⚠️ 남은 것: 시각 기록이 없어 **마지막에 누른 쪽이 이긴다.** 앱은 최신을 모른다

4. **자동화**: 열 때 당겨오고, 바뀔 때 밀어올린다. 여기서 위의 **동기 vs async**를 결정한다
   → 🎯 국면 B에서 유일하게 남은 것. 다만 수동으로 이미 쓸 만해서 **급하지 않다**
   ⚠️ 이제 덮어쓰기라 **자동화는 더 위험하다** — 자동 올리기가 반대편을 조용히 지운다.
   자동화에 손대면 "어느 쪽이 새 것인가"(`updatedAt`)를 **먼저** 정해야 한다

   > 🎯 **방향 확정 (2026-08-15): 모든 데이터 변경은 서버에 반영한다.**
   > 채널 켜고 끄기·추가·삭제·본 영상·풀 담기 — **바뀌는 순간 저장까지 끝난다.**
   > 「올리기」 버튼을 눌러야 안전해지는 지금 구조를 없애는 것이 목표다.
   > 수동 버튼은 그때까지의 임시 수단이지 도착지가 아니다.
   >
   > ⚠️ **착수 전에 정해야 하는 것 — 지금 구조로 그냥 자동화하면 사고가 난다:**
   > - 지금은 **파일 한 장을 통째로 덮어쓴다.** 여기에 자동을 붙이면 오래된 기기가
   >   앱을 여는 것만으로 반대편을 지운다. 수동일 땐 확인창이 막아주지만 자동엔 그게 없다
   > - **변경 단위로 쓰려면 통짜 파일이 맞지 않는다** — "채널 하나 껐다"를 반영하려고
   >   516개 풀을 통째로 올리는 꼴이다. 여기서 **진짜 DB(행 단위 쓰기)로 갈 이유**가 처음 생긴다
   > - 그러면 국면 B에서 접었던 **Supabase/D1 판단이 되살아난다**(위 벤더 표).
   >   ⭐ 그때 접은 이유는 "일이 많아서"가 아니라 **파일 한 장이면 충분해서**였다.
   >   그 전제가 깨지는 순간이 바로 이 항목이다 → **다시 비교할 것**
   > - 최소 선결 조건: `updatedAt`(어느 쪽이 새 것인가) · 오프라인에서 바뀐 것의 처리

- **옮겨갈 데이터**: `myChannels` · `watched` · `laterPool` · `playlistId` (= 백업 파일에 담기는 것 그대로)
  → **폰에서 담고 노트북에서 보는** 게 된다. 이게 국면 B의 실질적 보상.
- 배우는 것: OAuth 스코프 확장·증분 동의, Drive REST API, **파일 기반 동기화와 충돌 병합**
- 신호등: ✅ **켜짐 (2026-08-04)** — "폰에서 담아둔 걸 노트북에서 못 본다"가 실제로 답답해졌다

---

## 갈래 3 — 소비 방식 (모아둔 걸 실제로 보게 만들기)

수집·정리는 됐는데 **정작 안 보게 되는 문제**를 다루는 갈래.

**내 재생목록에서 랜덤 추천 — ✅ 완료 (2026-07-31, 방식 A-2)**
쌓아만 두고 안 보는 영상 더미에서 **무작위로 하나를 뽑아** "뭘 볼지 고르는 피로"를 없앤다.

⛔ **먼저 확인된 제약: 유튜브의 "나중에 볼"(WL)은 외부에서 읽을 수 없다.**
Google이 2016-09-12부로 막았다. OAuth로 본인 계정에 로그인해도 `playlistItems.list`가 **빈 목록**을 준다.
RSS도 `playlist_id=WL` → **404** (직접 테스트로 확인).
→ **다시 검토하지 말 것.** 목록이 없어진 게 아니라 API 경로만 막힌 것이다.

**그래서 채택한 방식**: 유튜브에서 **전용 재생목록**을 하나 만들어 그걸 "나중에 볼"처럼 쓴다.
영상 담을 때 `저장 → (그 재생목록 선택)`으로 한 번만 습관을 바꾸면 이후는 전부 자동.

- **읽기**: Worker에 `/playlist?id=PL...` 엔드포인트 추가 →
  YouTube Data API v3 `playlistItems.list`를 **페이지네이션으로 전부** 수집해 반환
  (RSS는 최근 15개뿐이라 랜덤의 의미가 줄어 API를 쓴다)
- ~~**API 키**: Worker 환경변수(Secret)에 보관 → 브라우저에 노출 안 됨.~~
  → **2026-07-31에 뒤집힘.** OAuth가 붙으면서 재생목록·영상 조회를 **브라우저가 Data API에 직접** 하도록
  일원화했다(`54736f0`). `/playlist`·`/videos` 엔드포인트와 `YT_API_KEY`를 Worker에서 삭제.
  Worker는 **RSS 전용**으로 남고, 존재 이유가 **CORS 하나만** 됐다.
  쿼터는 이제 내 계정에서 나간다(하루 10,000, `playlistItems.list`는 50개당 1 → 이 규모엔 남아돈다).
- **계약**: 기존과 **같은 모양**으로 돌려준다 `{ id, title, link, date, published }`
  → 화면의 렌더 함수를 그대로 재사용
- ~~**재생목록은 "일부 공개(unlisted)"**여야 한다. 완전 비공개는 OAuth가 필요해 범위 밖.~~
  → **제약 해소(2026-07-31)**: OAuth를 붙였으므로 **완전 비공개 재생목록도 읽힌다.**
  대신 풀을 **채우려면 로그인이 필요**해졌다(랜덤 뽑기·채널 목록은 로그인 없이도 된다).
- **랜덤 UX**: "랜덤으로 하나" 버튼 → 1개 제시, "다시 뽑기" 가능
- **`watched`와 연동** ⭐: 이미 본 영상은 후보에서 빼고 뽑는다 →
  지난 회차에 만든 본 영상 기록이 여기서 바로 값을 한다
- **캐싱**: 재생목록은 자주 안 바뀌므로 엣지 캐시 10분 정도
- ~~사전 준비(사용자 몫): Google Cloud Console에서 API 키 발급 + 재생목록 일부공개 설정~~
  → 이제 필요한 건 **OAuth 클라이언트 ID**와 전용 재생목록 생성뿐. 공개 범위는 아무래도 된다.
- 배운 것: 공식 API + 페이지네이션, ~~서버에 비밀값 숨기기~~ → **비밀값을 아예 안 갖는 구조**(OAuth), 무작위 선택 UX
- 국면 B와의 연결: 재생목록 ID·설정은 여전히 localStorage → **Drive 동기화 대상**(백업 파일에 이미 포함)

**⚠️ 실행하며 바뀐 것 — "516개를 옮기지 않는다"**
원래는 기존 "나중에 볼" 516개를 watchme 재생목록으로 **옮길** 생각이었으나, 따져보니 비용이 컸다:
- 자동 이전은 OAuth + `playlistItems.insert`가 필요한데 **1개당 50 units** → 516개면 25,800 units.
  하루 할당량 10,000이라 **3일에 걸쳐** 나눠야 함.
- Google Takeout은 **"나중에 볼"을 내보내지 않는다** (일반 재생목록만).

→ **전제를 뒤집었다.** 앱에 필요한 건 "재생목록에 들어있는 상태"가 아니라 **영상 목록 그 자체**다.
- **과거 516개**: WL 화면에서 ID만 뽑아 앱으로 직접 가져옴. `videos.list`는 **50개당 1 unit** → 약 11 units.
- **앞으로**: watchme 재생목록에 담고 앱이 동기화.
- 둘이 같은 풀로 합쳐진다. 유튜브 안에서 옮기는 작업은 **아예 하지 않는다.**

교훈: "A를 B로 옮겨야 한다"는 요구가 나오면, **정말 옮겨야 하는지 먼저 의심**한다.
필요한 건 대개 위치가 아니라 데이터다.

**내 재생목록 골라 쓰기 — 🎯 하기로 함 (2026-08-04 추가)**

지금은 재생목록을 쓰려면 **ID나 URL을 손으로 붙여넣어야** 한다(`#plInput`).
이미 로그인이 붙어 있는데도 **내가 가진 재생목록이 뭔지 앱이 모른다.** 로그인해놓고 ID를 찾아
붙여넣는 건 앞뒤가 안 맞는다 — 07-31에 구독 채널을 자동으로 가져오게 만든 것과 같은 종류의 결핍.

- **가져오기**: `playlists.list?mine=true` (`part=snippet,contentDetails`) →
  `{ id, title, count(itemCount), thumbnail }`. **1회 1 unit**이라 사실상 공짜
- **⭐ 새 부품이 거의 없다.** `youtube.js`의 `pages()`(페이지네이션 공통 루프)와
  `fetchSubscriptions`가 **그대로 본이 된다** — 같은 모양의 `mine=true` 목록 호출이다
- **스코프 추가 불필요.** `youtube.readonly` 하나로 이미 커버된다 → 동의 화면 재수신 없음
  (국면 B의 `drive.appdata`와 달리 이건 공짜다)
- **UI**: 재생목록 목록을 제목 + 영상 개수와 함께 보여주고, 고르면 `playlistId`에 넣고 동기화.
  손으로 붙여넣는 입력창은 **폴백으로 남긴다**(로그인 전이거나 남의 공개 재생목록을 쓸 때)
- ⛔ **WL은 여기에도 안 나온다.** `playlists.list`는 "나중에 볼"·"시청 기록"을 돌려주지 않는다.
  → watchme 전용 재생목록 우회는 **그대로 유지**된다. 이걸로 WL 문제가 풀리지 않는다
- ⚠️ **지금 저장 구조는 재생목록을 하나만 기억한다**(`KEYS.playlistId`). 여러 개를 풀에 넣고
  싶어지면 그건 저장 구조 변경이라 **별건**이다. 1차 범위는 "목록에서 골라 하나 지정"까지
- 배우는 것: 같은 페이지네이션 계약의 재사용, "선택"을 손입력에서 목록으로 바꾸는 UX

> 이 항목의 성격: **새 능력이 아니라 이미 가진 능력(로그인)의 회수**다.
> 국면 B(Drive)와 달리 새로 들이는 것이 하나도 없다 — API도 스코프도 벤더도.

---

## 한 줄 요약

- **공통 토대**: 데이터/화면 분리(계약 v1) ✅ → 릴레이 내재화(Worker) ✅ → 모듈 분리(app·worker·storage·youtube) ✅
  → 컬러 토큰·CSS 분리 ✅ → **화면 넷 분리(해시 라우팅) 뼈대** ✅
- **개인화**: localStorage(A) ✅ → 릴레이 내재화(A+) ✅ → 구글 로그인(A++) ✅ → 백업(A+++) ✅(→ 08-07 철거)
  → 기기 동기화 = Drive appData(B) ✅ 수동
- **소비**: 내 재생목록에서 랜덤 추천 ✅ (모아만 두고 안 보는 문제 해결)
- **스택**: 바닐라 JS → **React + Vite + TS + Tailwind** ✅ (2026-08-17), 배포 Pages → **Vercel** ✅
- 🎯 **지금**: 백엔드 ✅(2026-08-20 배포) → RSS 404 오진 수정 ✅(2026-08-21, `293e70b`)
  → **다음은 5번: 스키마 + 상호작용 단위 저장** (아래 전용 절)
  → 검색 UI(4번)는 그 뒤로 미뤘다. 검색의 결과물은 "채널 추가"인데 **채널이 어디 저장되는지를 5번이 바꾼다.**
  → 2026-08-17에 **첫 사용자 4명**(지인 테스트)이 생겼다. 여기서부터 "나 혼자 쓰는 앱"의 문제가 아닌
    항목이 처음 들어온다 — 피드백 창구가 그 첫 번째다.
- 프레임워크·백엔드는 **아플 때** 도입 (프로젝트 정신: 필요한 것만 그때그때)
  → ⭐ 2026-08-17에 React로 갔는데 **신호등이 바뀐 게 아니라 아픈 곳이 옮겨간 것**이다:
    "지금 화면"이 아니라 **"곧 할 작업"**이 아팠다(검색 UI·구독 분리가 전부 채널 화면에 얹힌다).
- ⭐ **벤더를 안 늘리는 쪽이었다** — 프록시는 내 Worker로, 데이터는 구글 계정 안으로.
  → ⚠️ **2026-08-17에 이 원칙을 의식적으로 깼다.** Vercel(배포) + Neon(DB 예정)이 늘었다.
    깬 근거: 원칙의 뜻은 "무조건 안 늘린다"가 아니라 **"필요 없는 부품을 안 들인다"**였고
    (08-04에 Drive를 고른 이유도 부품 수였다), 지금은 **멀티유저 + Spring Boot 이전**이
    계획으로 확정돼 있어 그 부품들이 필요해졌다.

## 🎯 개선안 4개 — 순서와 근거 (2026-08-17 확정)

| 순 | 항목 | 상태 |
|---|---|---|
| **1** | 프론트를 React+Vite+TS+Tailwind로 · 배포 Vercel | ✅ 완료 (`02bc62f`) |
| ~~2~~ | ~~구독 채널 / 내가 추가한 채널을 분리해서 표시~~ | ✅ 완료 (`ca7ff45`) — **분리보다 나은 답이 나왔다**(아래) |
| ~~3~~ | ~~제대로 된 백엔드 (Vercel Functions + **Neon** Postgres)~~ | ✅ **완료 · 배포됨** (2026-08-20, `d433bf0`). Spring Boot 이전은 그대로 남은 목표다 |
| ~~3-b~~ | ~~**의견 남기기 탭** — 3번의 첫 손님~~ | ✅ **완료 · 배포됨** — 리허설이 실제로 리허설이었다 (아래 «실제로 해보니») |
| **5** | **제대로 된 스키마 + 상호작용 단위 저장** (localStorage 원본 → 서버 원본) | 🎯 **다음** (2026-08-20 추가, 아래 전용 절) |
| **4** | 유튜브 검색 UI (채널명으로 실제 검색) | 대기 — **5번 뒤로 미룬다** (아래) |

**왜 이 순서인가**
- 1번이 먼저인 이유: 2·4번이 **전부 채널 화면에 얹힌다.** 바닐라로 먼저 만들면 이식 때 같이 버려진다.
- 2번이 4번보다 먼저인 이유: 검색의 결과물은 "채널 추가"인데 **어디에 추가되는지를 2번이 정한다.**
- 4번이 마지막인 이유: ⚠️ `search.list` = **100 units/회**(하루 100번). **멀티유저면 이 설계가 갈린다** —
  지금처럼 브라우저가 사용자 OAuth 토큰으로 부르면 쿼터가 **사용자별로 분산**되지만(100N회/일),
  서버 API 키로 옮기면 **전체가 한 통에 갇힌다**(100회/일 총합).
  → **검색은 사용자 토큰 유지, 백엔드는 캐시만 얹는 쪽**이 맞다. 그래서 3번이 먼저다.

⭐ **2번은 "분리"로 시작해 "안 저장"으로 끝났다** (2026-08-17). 처음엔 한 목록을 두 섹션으로
나누려 했는데, 실제로 319개를 훑어 33개를 고르고 나니 **구독은 목록이 아니라 "추가할 때
잠깐 여는 것"**이라는 게 보였다. 화면(모달)이 정해지자 저장 여부가 따라왔다 —
저장할 이유가 사라진 것이다. 로드맵이 *"구독은 후보, 저장할 필요도 없다"*고 08-05에
적어둔 것을 **UI 쪽에서 다시 만나 코드로 실현**한 셈이다.
→ ⚠️ **"실제로 써보는 일"이 설계를 바꿨다.** 1순위를 "기능이 아니라 쓰는 일"로 둔 값이
   여기서 청구됐다. 안 해봤으면 두 섹션짜리 화면을 만들고 끝냈을 것이다.

**전제 (2026-08-17 확정)**
- **멀티유저로 간다** — 나 혼자 쓰는 앱이 아니다. 스키마·쿼터 설계가 여기서 갈린다.
- **취준 포트폴리오를 겸한다** — 그래서 최종 목표는 Spring Boot다. 다만 먼저 동작을 만든다.
- ⭐ **2단계로 가도 낭비가 아닌 조건: DB를 Postgres로 고르는 것.**
  런타임(Vercel Functions → Spring Boot)을 갈아도 **데이터 이사가 없다.**
  Cloudflare D1(SQLite+Workers 전용)을 고르면 이 계획이 깨진다 → 그래서 Neon.
  ⚠️ Supabase는 무료 인스턴스 소진으로 제외.

## 💬 의견 남기기 (피드백 창구) — 🎯 3-b · 백엔드의 첫 손님 (2026-08-17 추가)

🚦 **신호등이 켜졌다 (2026-08-17).** 지인 4명에게 테스트 겸 임시 사용을 부탁했는데,
**의견을 카톡 같은 밖의 채널로 옮겨 전달해야 하는 것**을 불편해한다.

⭐ **이건 기능 요구가 아니라 테스트 인프라다.** 첫 사용자가 생긴 순간에만 켜지는 종류의 신호등이고,
지금까지의 항목들(전부 나 혼자 쓰는 앱의 문제)과 성격이 다르다.

**밖의 채널로 받을 때 실제로 드는 비용** — "불편하다"를 쪼개보면 둘이다.
- 보내는 쪽: 앱을 나가서 다른 앱을 열고, 무슨 화면이었는지 **말로 재구성**해야 한다 → 그래서 안 보낸다
- 받는 쪽: "어느 화면에서? 로그인은 했었나? 폰이야 노트북이야?"를 **되묻는 왕복**이 매번 붙는다
  → ⭐ 그래서 이 기능의 값은 입력창이 아니라 **상황을 자동으로 같이 보내는 것**에 있다

### 왜 3번(백엔드)의 첫 손님으로 이게 맞나

백엔드를 세우면 "무엇을 제일 먼저 서버로 옮기나"를 고르게 되는데, 후보 둘의 위험이 딴판이다.

| 첫 테이블 후보 | 쓰기 방향 | 사용자 식별 | 충돌 | 잘못됐을 때 | 판단 |
|---|---|---|---|---|---|
| **feedback** | **삽입 한 방향** | 익명 허용 | **없다** (행끼리 독립) | 의견 몇 건 잃는다 | ✅ **먼저** |
| `myChannels`·`laterPool` 이사 | 읽기+쓰기 | 필요 (멀티유저) | Drive와 **이중 소유**, `updatedAt` 미정 | 내 데이터 전부 | ❌ 뒤 |

⭐ **즉 이건 백엔드를 늦추는 곁가지가 아니라, 백엔드의 가장 싼 리허설이다.**
Neon 연결 · 스키마 · 마이그레이션 · Vercel Function 배포 · 환경변수 · 입력 검증을
**잃어도 되는 데이터로** 한 바퀴 돌린다. 그리고 Spring Boot 이전 때 **첫 컨트롤러로 그대로 옮겨진다**
(테이블 하나, POST 하나 — 이전 리허설로도 제일 작다).

⚠️ **반대 방향의 함정**: 이걸 하려고 3번을 미루면 안 된다. 창구는 3번 **안에** 들어가는 것이고,
따로 세우면 서버를 두 번 만든다. 3번 착수가 며칠 밀린다면 아래 "급하면"을 본다.

### 계약을 먼저 정한다 (목적지를 갈아 끼울 수 있게)

```
POST /api/feedback
{ "kind": "bug" | "idea" | "etc",
  "body": "…",                        // 필수, 1~1000자
  "nickname": "",                     // 선택
  "meta": { "screen": "#/feed", "signedIn": true, "channels": 33,
            "viewport": "390x844", "ua": "…", "build": "커밋 해시" } }
→ 201 { "ok": true }
```

**에러 계약은 2026-08-16 RSS 계약의 정신 그대로** — 상태코드가 뜻을 갖게 한다.

| | 뜻 | 화면이 할 말 |
|---|---|---|
| 400 | 입력 문제 (비었음·너무 김) | 그대로 보여준다 (사용자가 고칠 수 있다) |
| 429 | 너무 자주 보냈다 | "잠깐 뒤에 다시" |
| 500 | 진짜 장애 | "보내지 못했다" — 원인은 사용자 몫이 아니다 |

- ⭐ **실패해도 입력한 글을 지우지 않는다.** 초안을 localStorage에 남긴다 —
  사람이 쓴 글을 네트워크 실패로 날리는 게 이 기능에서 낼 수 있는 최악의 사고다.
- 경계는 `lib/worker.ts`와 같게: **판정만 넘기고 한국어 문장은 화면이 만든다.**

### 화면 (① 1차 범위)

- 탭 다섯째 `#/feedback`. `SCREENS` 배열에 한 줄 + `App.tsx`에 분기 하나 → **라우팅은 이미 있다.**
- ⚠️ **탭이 5개가 되면 `flex-1` 폭이 20%로 준다** (폰 375px에서 ~72px). 「💬 의견」이 사실상 한계다.
  넘치면 이모지를 떼거나 다른 라벨을 줄이는 게 먼저 — 스크롤 탭·더보기 메뉴는 5개엔 과하다.
- 입력: **유형**(버그/제안/기타) · **본문**(textarea, 필수) · **닉네임**(선택 — "누가 썼는지 알려주고 싶을 때만")
- 보낸 뒤: 감사 문구 + **방금 보낸 것을 로컬에 남긴다**(`myFeedback`) → 자기가 뭘 보냈는지 확인된다.
  ⚠️ 이건 `channelCache`/`subsCache`와 **같은 지위** — `exportAll()`에 넣지 않는다.
  (`storage.ts`의 KEYS 주석 그대로: 여기 넣으면 드라이브로 실려 나간다)

**무엇을 자동으로 붙이고 무엇을 안 붙이나** — 이 기능의 값과 위험이 둘 다 여기 있다.

- 붙인다: 현재 화면 · 로그인 여부 · **켠 채널 수** · viewport · UA · 빌드(커밋 해시)
- ⛔ **안 붙인다: 채널 목록 · 풀 · 본 영상 기록.** 그 사람이 무엇을 보는지는 개인정보다. **개수만** 보낸다
- ⛔ **이메일을 필수로 받지 않는다.** 4명이라 답장 경로는 이미 있다 —
  여기서 받아야 하는 건 연락처가 아니라 **상황**이다
- ⚠️ 화면에 "무엇이 함께 전송되는지" 한 줄로 보여준다. 몰래 붙이면 그게 곧 사고다

### 읽는 쪽 — 1차 범위에서 **만들지 않는** 것

- 앱 안의 의견 목록 · 상태(읽음/반영) · 답장 → **없다.** Neon 콘솔에서 SQL로 읽는다
- **알림은 예외로 넣는다.** 아무도 폴링하지 않으면 의견이 와도 모른다 → insert 뒤
  웹훅 한 줄(디스코드/텔레그램). ⚠️ **웹훅 실패가 insert를 되돌리면 안 된다** —
  저장이 본체, 알림은 곁가지다 (try/catch로 삼킨다)
- 신호등(목록 화면·상태·답장): **같은 의견을 두 번 읽고 헷갈릴 때.** 4명·하루 몇 건에는 SQL이 이긴다

### 스키마 초안 · 남용 방어

```sql
create table feedback (
  id          bigserial primary key,
  created_at  timestamptz not null default now(),
  kind        text not null,
  body        text not null,
  nickname    text,
  meta        jsonb not null default '{}'
);
```

- ⛔ **`user_id` 컬럼을 지금 만들지 않는다.** 멀티유저가 오면 `ALTER`로 붙인다 (필요한 것만 그때그때)
- ⛔ **IP 원본을 저장하지 않는다.** 레이트 리밋에 필요하면 해시만
- 방어는 셋으로 충분: **Origin 제한**(Worker `ALLOWED_ORIGINS`와 같은 방식) · 본문 길이 상한 · 분당 건수 제한.
  캡차·로그인 강제는 4명 규모에 과하다 → 신호등: **실제로 스팸이 들어올 때**

### 급하면 (3번이 길어질 때만 — 하루짜리 우회로)

| 안 | 드는 일 | 판단 |
|---|---|---|
| **Vercel Function + Neon** (본안) | 중간 | ✅ **채택.** 3번이 이미 다음이고 **새 벤더가 0개**다 |
| 구글 폼을 탭에 `iframe` | 제일 적다 | △ 창구는 즉시 생긴다. ⚠️ 하지만 **메타 자동 첨부가 안 된다**(=진짜 값이 빠진다). 그리고 `fetch`로 보내면 CORS에 막혀 **성공/실패를 알 수 없다**(`no-cors`는 응답을 못 읽는다) → 통째로 띄우는 것만 정직하다 |
| Worker에 POST + 채팅 웹훅 | 적다 | △ 벤더 0·DB 0. ⚠️ **Worker가 RSS 전용이라는 07-31의 정리를 깬다.** 받는 곳이 또 채팅이라 목록·검색이 없다 → 문제를 반쪽만 푼다 |

→ **본안 하나로 간다.** 임시 수단은 **버릴 화면을 한 번 더 만드는 값**이다.

**배우는 것**: 첫 POST 경로(입력 검증 → DB insert), 서버 쪽 에러 계약(400/429/500), `jsonb`,
레이트 리밋, 웹훅 알림, Neon 연결 관리(서버리스의 커넥션 문제).
→ ⭐ **내가 만든 서버에 쓰는 것은 이게 처음이다.** Drive 쓰기는 남의 API였고
Worker는 무상태 읽기 릴레이였다.

### 실제로 해보니 (2026-08-20 · 로컬 완주)

계약(POST 모양·400/429/500·DDL)은 **적어둔 그대로 코드가 됐다.** 달라진 건 넷이고, 넷 다
글로 적을 땐 안 보이던 것이다.

| 무엇 | 계획 | 실제 | 왜 |
|---|---|---|---|
| `ip_hash` 칸 | DDL에 없음 | **추가** | 서버리스는 요청마다 다른 인스턴스라 분당 건수를 **메모리로 못 센다** → 세는 근거가 DB에 있어야 한다. ⛔ IP 원본이 아니라 sha256(IP+소금) |
| 어느 헤더로 IP를 읽나 | 안 정함 | `x-real-ip` **먼저**, 없으면 `x-forwarded-for`의 **맨 뒤** | ⚠️ `x-forwarded-for` **맨 앞은 보내는 쪽이 쓴 값일 수 있다.** 그걸 세면 헤더 한 줄 바꿔가며 제한을 무력화한다 |
| 초안 저장 시점 | "실패하면 남긴다" | **쓸 때마다** | 이 앱은 탭을 옮기면 화면이 언마운트된다 → **실패하기도 전에** 글이 날아간다. 로드맵이 막으려던 사고가 실패 경로 밖에도 있었다 |
| `vercel.json` | 없음 | `{"framework":"vite"}` | 프로젝트 설정의 프리셋이 `null`이라 **배포는 서버가 알아서 감지해 잘 됐지만** 로컬 `vercel dev`는 정적 사이트로 오인해 옛 기본값 `yarn`을 부르다 죽었다. ⭐ 자동 감지에 기대던 것이 로컬에서만 터진 셈 |

**로컬에서 증명된 것**: 201 · 400 세 갈래(빈 본문·모르는 kind·1001자) · **1분 창 안 5건 초과 시 429** ·
meta 화이트리스트가 모르는 키를 버림 · 화면에서 보내기 → 목록에 사본.
**로컬에서 증명 못 한 것**: ⚠️ **IP별 분리.** `vercel dev`가 헤더를 자기 것으로 덮어써
모든 요청이 한 사람으로 세어진다 → 배포 후에만 확인된다.

**배포 뒤 확인 (2026-08-20, `8f393d5`)**: 201 · 허용 안 된 Origin은 403 · **429까지 재현** ·
프로덕션 행들이 로컬과 **다른 ip_hash로 묶임**(=실제 클라이언트 IP가 해시되고 있다 → 로컬에서
못 본 IP별 분리가 여기서 확인됐다).

⚠️ **거리가 비용이었다 — 배포하고 재보니 POST 하나에 0.8~2.1초.**
요청은 서울로 들어오는데(icn1) 함수는 미국 동부에서 돌고(iad1) Neon은 싱가포르에 있었다.
POST 하나가 `count` + `insert` **두 질의**라 태평양을 두 번 건넜다.
→ 함수를 **DB 옆(sin1)으로** 옮겨 0.5~0.7초. ⭐ 규칙 한 줄로 남긴다:
**사용자와의 거리는 한 번 붙고, DB와의 거리는 질의 수만큼 붙는다.** 그래서 함수는 DB 옆에 세운다.
- ⬜ 남은 지연의 일부는 **디스코드 웹훅을 기다린 뒤 응답하기 때문**이다. 알림은 곁가지이므로
  응답 후로 미룰 수 있다(`waitUntil`). 0.5초가 거슬릴 때가 신호등 — 지금은 폼 하나에 충분하다.

**잃는 것 (정직하게)**: 의견이 구글 계정 밖(Neon)에 쌓인다 → 내 데이터 저장소가 **둘**로 갈린다.
그리고 창구를 열면 **답장 요구가 따라온다** — 목록·상태·알림이 그 뒤에 줄 서 있다는 뜻이다.

---

## 🗄️ 스키마 + 상호작용 단위 저장 — 🎯 5번 (2026-08-20 추가)

한 문장으로: **원본을 브라우저에서 서버로 옮기고, 누른 것이 곧 저장이 되게 한다.**
「☁️ 올리기」를 눌러야 안전해지는 구조를 없애는 일이다.

⚠️ **이름을 먼저 좁혀둔다.** "실시간"이라 부르지만 **내 다른 기기에 즉시 밀어주는 것(푸시)이
아니다.** 여기서 뜻하는 건 **상호작용 하나 = 저장 하나**다. 푸시는 아래 «안 만드는 것» 참고.

### 🚦 신호등 (셋 다 이미 켜졌다)

- **눌러야 안전해진다** — 올리기를 안 누르고 브라우저 데이터를 지우면 그대로 사라진다
- **어느 쪽이 최신인지 앱이 모른다** — 시각 기록이 없어 *마지막에 누른 쪽이 이긴다*
- **멀티유저가 전제다** — 테스터 4명이 이미 있다. 지금 구조엔 사용자라는 개념이 아예 없다
- ⭐ 그리고 **리허설이 끝났다**(3-b). Neon 연결·스키마·마이그레이션·환경변수·에러 계약을
  잃어도 되는 데이터로 한 바퀴 돌려봤다. 이번엔 **잃으면 안 되는 데이터**로 같은 길을 간다

### 무엇이 뒤집히나 — 소유권의 이동

| | 지금 | 5번 뒤 |
|---|---|---|
| `localStorage` | **원본** | **캐시** (없어도 서버에서 다시 온다) |
| 드라이브 | 유일한 사본 | ⬜ 출구로 남길지 은퇴시킬지 — 아래 |
| 서버(Neon) | 의견만 | **원본** |

⭐ **`storage.ts`가 이 날을 예고해뒀다.** 파일 첫 주석의 *"② 로그인 → 서버 저장 — 저장 위치를
브라우저에서 서버로 갈아끼워야 한다. ②는 이 파일의 함수 속만 바꾸면 되도록"* 이 그것이다.
화면이 `localStorage`를 직접 안 부르게 막아둔 값이 **여기서 청구된다** — 바꿀 곳이 한 파일이다.
⚠️ 다만 **완전히 공짜는 아니다**: 저장이 동기 함수에서 **비동기 요청**으로 바뀐다.
`saveChannels(list)`가 즉시 끝나던 자리가 전부 "기다리거나, 실패할 수 있는 것"이 된다.

### 사용자를 무엇으로 식별하나

**구글 OAuth의 `sub`** (계정마다 고정된 ID). ⛔ 이메일은 안 쓴다 — 바뀔 수 있다.

⚠️ **여기가 이번에 새로 드는 진짜 일이다.** 지금은 브라우저가 구글에서 받은 토큰으로
구글을 직접 부를 뿐이라, **서버가 "이 요청이 누구인지" 판단해본 적이 없다.**
→ ~~로그인 때 **ID 토큰**을 같이 받아 서버가 서명을 검증하고 `sub`를 꺼낸다.~~
⛔ 클라이언트가 보내는 `user_id`를 그대로 믿으면 안 된다 — 남의 데이터를 마음대로 읽고 쓴다.

⚠️ **위 한 줄이 2026-08-25에 틀린 것으로 밝혀졌다** — 지금 로그인 흐름은 ID 토큰을 **주지 않는다.**
어떻게 받을지를 새로 정해야 했고, 그 결과가 바로 아래 절이다.

⬜ **정해야 할 것: 비로그인 사용자.** 지금은 로그인 없이도 채널을 보고 켜고 끌 수 있다.
   ⓐ 비로그인은 로컬 전용, 로그인하면 서버로 승격 (지금 경험이 안 깨짐 · 코드 두 벌)
   ⓑ 로그인 필수 (단순함 · 진입 문턱이 생긴다)
   → **ⓐ가 맞아 보인다.** localStorage가 어차피 캐시로 남으므로 "로그인 안 하면 캐시만 쓴다"가
     자연스럽고, 4명에게 "쓰려면 로그인부터"를 요구하는 건 지금 단계에서 손해다.

### 🔐 인증 설계 확정 (2026-08-25)

⚠️ **먼저: 위에 적어둔 계획이 성립하지 않았다.** *"로그인 때 ID 토큰을 같이 받아"*라고 썼는데,
지금 쓰는 GIS **토큰 모델**(`initTokenClient`)의 `TokenResponse`에는 **`id_token` 필드가 없다.**
`access_token`·`expires_in`·`hd`·`prompt`·`token_type`·`scope`·`state`·`error*`가 전부다.
→ 토큰 모델은 **인가(authorization)**용이라 **신원(authentication)**을 안 준다.
⭐ *"OAuth는 인증 프로토콜이 아니다"*가 여기서 구체적으로 걸린 것이다.

#### 세 갈래와 판단

| | 무엇을 하나 | 왜 안 골랐나 / 골랐나 |
|---|---|---|
| ⓐ GIS 신원 로그인 추가 (`google.accounts.id`) | 진짜 ID 토큰을 따로 받는다 | 안전하지만 **로그인 UI가 둘로 갈린다**(신원 + 권한). "버튼 하나" 경험이 깨진다 |
| ⓑ 액세스 토큰을 서버가 introspect (`tokeninfo`) | 지금 토큰을 그대로 서버가 검증 | ⛔ **`aud` 검사를 빼먹어도 평소엔 멀쩡히 동작한다**(gotcha 22와 같은 종류). 요청마다 구글 왕복. 구글이 죽으면 내 앱도 죽는다. **인증 수명이 인가 수명에 묶인다** |
| ✅ **ⓒ Authorization Code flow** | 브라우저는 code만 받고 **서버가 교환** | ⓐ의 안전성 + ⓑ의 UX. 게다가 **백엔드가 생겨서 처음 가능해진 선택지다** — 08-20 전엔 시크릿을 둘 곳이 없었다 |

⭐ **ⓑ의 함정을 특히 적어둔다.** 액세스 토큰은 bearer라 **다른 앱이 받은 구글 토큰도 문자열로는
똑같이 생겼다.** 악성 앱이 자기 사용자의 토큰을 내 서버로 보내면 `tokeninfo`는 그 사람의 `sub`를
정직하게 돌려주고, 내 서버는 통과시킨다. 막는 건 `aud` 검사 하나뿐인데 **내 토큰으로 테스트하면
항상 통과**한다 → 틀린 검사가 조용히 산다. ID 토큰은 `aud`가 **서명된 페이로드 안에** 있어
빼먹기 어렵게 설계돼 있다.

⭐ **그 자랑("시크릿은 아예 안 쓴다")은 제약의 미화였다.** `youtube.ts` 주석의 *"정적 사이트의
대가"*가 그걸 실토하고 있었다. 서버가 생기면서 제약이 사라졌다.

#### 문서로 확인한 것 (2026-08-25) — 둘은 예상이 틀렸다

| | 결과 |
|---|---|
| 팝업 모드 | ✅ `ux_mode` **기본값이 `popup`**. 콜백이 `{code, scope, state, error*}`. 페이지가 안 튕겨서 **해시 라우팅·화면 상태가 유지된다** |
| 교환 때 `redirect_uri` | ❌ **`postmessage`가 아니다**(그건 옛 `gapi` 방식). 팝업 모드에선 **호출한 페이지의 origin**이 된다 → `https://youtube-feed-mu.vercel.app`. 틀리면 `redirect_uri_mismatch`인데 이 에러는 원인이 여섯 가지라 진단이 오래 걸린다 |
| 서명 검증 | ⭐ **필요 없다.** 구글 문서: *"communicating directly with Google over an intermediary-free HTTPS channel and using your client secret … you can be confident that the token really comes from Google"* → JWKS도 `jose` 의존성도 왕복도 0. **단 `iss`·`aud`·`exp`는 검사한다** |
| 그 면제의 조건 | ⚠️ *"if your server passes the ID token to other components, … validate the token"* → **ID 토큰을 아무 데도 안 넘기는 설계를 유지하는 것**이 면제의 조건이다. 받아서 `sub`만 꺼내고 버린다 |
| `openid` 스코프 | 필수(OIDC 정의) → **재동의 1회**. 나·테스터 4명 |
| 승인된 리디렉션 URI | 🟡 팝업은 등록 불필요라고 **명시돼 있지 않다** → origin을 등록해두고 시작한다. 비용 0 |

#### refresh 토큰 — ⬜ **1차 범위 밖** (2026-08-25 결정)

⚠️ `initCodeClient` 설정에 **`access_type`가 없어서 끄는 방법이 없다.** 구글이 줄 것이고
**우리는 받자마자 버린다.** → 코드 주석에 명시할 것. 없으면 다음에 보는 사람이 버그로 오해한다.

**왜 안 저장하나** — 구글 토큰이 필요한 일은 셋뿐이고 전부 *어쩌다 하는 일*이다:
「구독에서 고르기」(채널 추가할 때) · 재생목록 동기화(가끔) · 드라이브(**5번 이후 은퇴 예정**).
나머지 전부는 앱 세션만 있으면 된다.
→ 저장해서 사는 것은 **「1시간마다 버튼 한 번」 → 「7일마다 버튼 한 번」**뿐이다.

⚠️ **7일인 이유**: 이 앱은 OAuth 동의 화면이 **「테스트 중」** 상태고(gotcha 15), 구글은 그 상태의
프로젝트에 **7일 만료 refresh 토큰**을 준다. 벗어나려면 게시해야 하는데 `youtube.readonly`가
**민감 스코프**라 구글 검증 절차가 붙는다 — 5번과 별개의 큰 일이다.

**값**: ⛔ 만료 없다시피 한 상시 접근 권한이 내 DB에 앉는다(유출 시 그 사람의 유튜브·드라이브에
계속 접근) · 암호화 여부 결정 · 권한 철회·계정 삭제 시 지우는 로직(구글이 명시적으로 요구) ·
갱신 실패 경로 하나 추가.

🚦 **신호등**: ⓐ 사용자가 **앱을 안 열고 있는 동안** 구글을 불러야 할 때(알림·자동 동기화)
ⓑ 테스터에게서 *"자꾸 로그인하라고 뜬다"*는 말이 실제로 나올 때.
→ ⭐ **미루는 비용이 낮다** — 나중에 `prompt=consent`로 언제든 받을 수 있다.

#### 앱 세션 — ⭐ **내 서버가 처음으로 발급 주체가 된다**

지금 이 서비스에는 **「내가 발급한 자격증명」이 하나도 없다.** 구글 것을 받아 쓰거나(액세스 토큰),
아무 자격증명도 없다(Worker·의견 API). `sub`를 알아내는 건 **로그인 순간 한 번**이므로,
그 뒤 요청마다 *"이게 누구지"*를 답할 것이 따로 필요하다. 매번 구글에 물으면 **ⓑ로 되돌아간다.**

- **HttpOnly 쿠키** — localStorage에 두면 XSS 하나에 세션이 통째로 털린다. 이 앱은 이미
  `textContent`로 XSS를 막고 있는데(30-cors 7절) 세션을 거기 두면 **그 방어가 무의미해진다**
- ⚠️ 쿠키는 CSRF를 부른다 → `SameSite=Lax`. ⭐ API가 `/api/*`로 **같은 origin**이라 깔끔히 듣는다 —
  08-17 Vercel 이전으로 공짜로 생긴 이점이다
- **무상태 JWT가 아니라 DB 세션 행** — JWT는 **로그아웃이 안 된다**(블랙리스트를 따로 만들어야 한다).
  DB는 이미 있고 행 하나 지우면 즉시 끝난다
- 수명 **30일 슬라이딩**. 짧게 잡을 이유가 없다 — 세션이 죽어도 구글 재로그인이 아니라 앱 재로그인이다

#### 발급 주체와 수명 (한눈에)

| 자격증명 | 발급 | 누가 갖나 | 수명 |
|---|---|---|---|
| 인가 코드 | 구글 | 브라우저 (찰나) | 일회용·짧음 (구글이 값을 문서화 안 함) |
| **ID 토큰** | 구글 | **서버** (찰나) | 1시간이지만 **1초도 안 쓴다** — `sub` 꺼내고 폐기 |
| 액세스 토큰 | 구글 | 브라우저 | 1시간 |
| refresh 토큰 | 구글 | ⛔ **아무도 — 버린다** | (테스트 모드 7일) |
| **앱 세션** | ⭐ **내 서버** | 브라우저 (쿠키) | **내가 정한다** (30일) |
| Worker 접근 | 없음 | — | Origin 검사만 |

#### 만들 것 (구현 순서)

1. `db/002_auth.sql` — `app_user`(`google_sub` unique) + `session`
2. `api/auth.ts` — code 교환 → `iss`·`aud`·`exp` 검사 → `sub` → upsert → 세션 쿠키
3. 화면 — `initTokenClient` → `initCodeClient`, 팝업 콜백이 code를 `/api/auth`로 POST
4. 콘솔 — 승인된 리디렉션 URI에 origin 등록 · `openid` 추가 → **재동의 1회**

⚠️ **2번을 세우는 순간 «refresh 토큰이 실제로 오는가»가 자동 실측된다.** 어느 쪽이든 결정은
안 바뀐다 — 오면 버리고, 안 오면 애초에 없던 것이다.

### 스키마 초안

```sql
create table app_user (
  id          bigserial primary key,
  google_sub  text not null unique,      -- 구글이 주는 고정 ID
  created_at  timestamptz not null default now()
);
-- ⛔ refresh_token 칸을 만들지 않는다 (2026-08-25 결정). 위 «인증 설계 확정» 참고 —
--    구글이 주지만 우리는 버린다. 만드는 순간 «만료 없는 상시 접근 권한 보관»이 시작된다.

create table session (                    -- ⭐ 내 서버가 발급하는 유일한 자격증명
  id          text primary key,           -- 난수. 쿠키에 들어가는 값
  user_id     bigint not null references app_user(id) on delete cascade,
  created_at  timestamptz not null default now(),
  seen_at     timestamptz not null default now(),  -- 슬라이딩 만료의 기준
  expires_at  timestamptz not null
);
-- ⭐ 무상태 JWT가 아니라 행으로 두는 이유는 **로그아웃**이다. JWT는 발급하면 못 거둔다
--    (블랙리스트를 따로 만들어야 한다). 행은 지우면 그 순간 끝이다.
create index if not exists session_expiry_idx on session (expires_at);

create table channel (                    -- = myChannels 한 줄
  user_id     bigint not null references app_user(id) on delete cascade,
  channel_id  text   not null,
  topic       text   not null,
  name        text   not null,
  active      boolean not null default true,
  updated_at  timestamptz not null default now(),
  primary key (user_id, channel_id)
);

create table watched (                    -- = watched { 영상ID: 시각 }
  user_id    bigint not null references app_user(id) on delete cascade,
  video_id   text   not null,
  watched_at timestamptz not null default now(),
  primary key (user_id, video_id)
);

create table later_pool (                 -- = laterPool
  user_id    bigint not null references app_user(id) on delete cascade,
  video_id   text   not null,
  title text, link text, published text, channel text,
  source     text,
  added_at   timestamptz not null default now(),
  primary key (user_id, video_id)
);

create table user_setting (               -- 지금은 playlistId 하나뿐이다
  user_id     bigint primary key references app_user(id) on delete cascade,
  playlist_id text,
  updated_at  timestamptz not null default now()
);
```

- ⭐ **복합 PK(`user_id, video_id`)가 곧 멱등성이다.** 같은 영상을 두 번 봤다고 눌러도
  `on conflict do nothing`이면 사고가 안 난다. 재시도를 마음 놓고 할 수 있게 된다
- ⛔ `channelCache`·`subsCache`는 **서버에 올리지 않는다.** 다시 만들 수 있는 것은 계약에 안 넣는다
- ⚠️ `watched`는 지금 상한 1000이다(오래된 것부터 버림). 서버로 가면 그 이유(용량)가 사라지니
  **상한을 없앨지 정해야 한다.** 없애는 쪽이 맞아 보이지만, 그러면 피드 렌더가 읽는 양이 는다

### 쓰기 단위 = 상호작용 하나

| 행동 | 요청 |
|---|---|
| 채널 켜고 끄기 | `PATCH /api/channels/:channelId { active }` |
| 채널 추가·삭제 | `POST` / `DELETE /api/channels/:channelId` |
| 영상 봤음 | `POST /api/watched { videoId }` |
| 풀에 담기 | `POST /api/pool` (재생목록 동기화 = **여러 건 한 번에**) |

- ⭐ **낙관적 UI로 간다**: 화면을 먼저 바꾸고 요청을 뒤에 보낸다. 실패하면 되돌리고 말해준다.
  안 그러면 토글 하나에 왕복(현재 실측 0.5초)을 기다리게 되고, **지금보다 느려진 앱**이 된다
- ⚠️ **「전부 끄기/켜기」가 이 설계의 지뢰다.** 지금은 localStorage 한 번 쓰기지만 상호작용
  단위로 옮기면 **수백 요청**이 된다 → **벌크 엔드포인트를 처음부터 같이 만든다**
  (`PATCH /api/channels { ids: [...], active }`). 이건 선택이 아니라 필수다
- ⬜ 연타·중복 요청: 같은 채널을 빠르게 토글하면 순서가 뒤집힐 수 있다. 마지막 값만 보내는
  디바운스로 시작하고, 그래도 어긋나면 그때 큐를 만든다

### 실패가 처음으로 생긴다

지금 저장은 **실패하지 않는다**(localStorage 동기 쓰기). 서버로 옮기면 저장이 실패할 수 있다.
⭐ **이 전환의 진짜 비용은 스키마가 아니라 이것이다.**

- 낙관적 UI를 되돌릴 때 **무엇이 안 저장됐는지 사람에게 보여준다** (조용히 되돌리면 최악이다)
- 에러 계약은 3-b와 같은 정신: 401 로그인 만료 · 400 입력 · 409 충돌 · 500 장애
- ⚠️ ~~**토큰이 1시간이고 refresh가 없다**(youtube.ts). 서버 저장이 로그인에 매달리는 순간
  "쓰다가 1시간 뒤 전부 401"이 현실이 된다 → **refresh 토큰이 이때 필요해진다**~~
  → ⭐ **진단이 반만 맞았다** (2026-08-25). **내 데이터를 쓰는 데 구글 토큰은 필요 없다** —
  채널 토글·본 영상·풀은 전부 내 DB 일이다. 서버 저장이 매달리는 것은 구글 로그인이 아니라
  **내 앱 세션**이고, 그 수명은 **내가 정한다.** → 이 문제를 푸는 건 refresh 토큰이 아니라
  세션이다. 아래 «인증 설계 확정» 참고
- ⬜ 오프라인 큐: 지금은 안 만든다. 신호등 — 실제로 지하철에서 쓰다 잃을 때

### 옮기는 방법 (마이그레이션)

`exportAll()`이 그대로 이사 수단이 된다 — **08-17 origin 이사 때 쓴 수법 그대로**.
`POST /api/import`에 JSON 한 장을 보내고 **서버가 행으로 펼친다**. 한 번만 도는 길이다.
⚠️ 두 번 눌러도 안전해야 한다 → `on conflict` 로 멱등하게. (복합 PK가 여기서 값을 한다)

### 안 만드는 것 (1차 범위 밖)

- ⛔ **실시간 푸시(WebSocket·SSE).** 다른 기기에 즉시 반영하는 것은 이번 목표가 아니다.
  신호등: **두 기기를 동시에 열어두고 번갈아 쓸 때.** 그전엔 새로고침이면 충분하다
- ⛔ **드라이브 즉시 은퇴.** 서버가 원본이 돼도 당분간 남긴다 — `appDataFolder`는 사람이
  못 열지만, 그래도 **구글 계정 밖으로 나가는 유일한 출구**였다.
  ⬜ 은퇴 시점은 "서버 백업/내보내기를 만든 뒤"로 미룬다
- ⛔ 협업·공유. 멀티유저는 **격리**를 뜻하지 공유를 뜻하지 않는다

### 왜 4번(검색 UI)보다 먼저인가

08-17에 *"2번이 4번보다 먼저인 이유: 검색의 결과물은 «채널 추가»인데 **어디에 추가되는지를
2번이 정한다**"* 라고 적었다. **같은 논리가 5번에도 그대로 걸린다** — 5번이 «채널 추가»의
저장 위치를 통째로 바꾼다. 4번을 먼저 만들면 그 부분을 두 번 쓴다.
✅ 4번의 쿼터 결론(*검색은 사용자 토큰 유지, 백엔드는 캐시만*)은 5번과 충돌하지 않는다 —
검색 호출은 계속 브라우저가 하고, 서버는 결과 캐시와 «추가»만 받는다.

**배우는 것**: 스키마 설계(복합 PK·외래키·`on delete cascade`), **서버 쪽 인증**(ID 토큰 검증),
낙관적 UI와 되돌리기, 부분 실패, 멱등한 마이그레이션, 벌크 API.
→ ⭐ 3-b가 "쓰기 한 방향·잃어도 되는 데이터"였다면, 이건 **읽기+쓰기·잃으면 안 되는 데이터**다.
   로드맵이 08-17에 «먼저/뒤»로 갈라놨던 그 «뒤»가 지금이다.

**잃는 것 (정직하게)**: 저장이 동기에서 비동기로 바뀌어 **모든 저장 지점에 실패 경로가 생긴다.**
그리고 앱이 **내 서버 없이는 못 도는 것**이 된다 — 지금까지는 Worker가 죽어도 저장은 멀쩡했다.

---

### 다음에 할 것 (2026-08-17 기준)

| 순위 | 항목 | 왜 이 순서인가 |
|---|---|---|
| ~~-~~ | ~~재생목록 랜덤 추천 (A-2)~~ | ✅ 완료 (2026-07-31) |
| ~~-~~ | ~~구글 로그인(OAuth) + 구독 가져오기~~ | ✅ 완료 (2026-07-31). 단 이건 **읽기 인가**라 그 자체로는 동기화가 아니다 |
| ~~-~~ | ~~내보내기/가져오기~~ | ✅ 완료 (2026-08-02) — 이민 수단이 생겼다. ⚠️ **2026-08-07에 지웠다**(`68ec3bc`) — 드라이브로 갈음. `exportAll`은 그대로 살아 동기화가 쓰고, `importAll`(합치기)은 **2026-08-15에 지웠다**(`ee2a4c1`) — 그 자리는 `replaceAll`(교체)이 대신한다 |
| ~~-~~ | ~~기기 동기화(국면 B) — Drive appData~~ | ✅ 수동 올리기/내려받기 완료 (2026-08-05, `3f20d6b`). 자동화(4단계)는 남았다 |
| ~~-~~ | ~~컬러 변수화~~ | ✅ 완료 (2026-08-05). 토큰 7개로 정리, **보이는 변화 0을 측정해 확인**(요소 24개 전후 비교) |
| ~~-~~ | ~~화면 분리 + 해시 라우팅 (갈래1 2.5단계)~~ | ✅ **뼈대** 완료 (2026-08-07, `68ec3bc`). 화면 넷·탭·해시·피드 지연 렌더. **내용물은 아래 1~3번** |
| ~~-~~ | ~~채널 관리 화면 — 구독에서 고르기(`active` 플래그)~~ | ✅ 완료 (2026-08-15, `e3931dd`). 목록·토글·전부 끄기/켜기. **꺼진 채널은 Worker 요청조차 안 나간다** |
| ~~-~~ | ~~프론트 React+Vite+TS+Tailwind 이식 · Vercel 이전~~ | ✅ 완료 (2026-08-17, `02bc62f`). `lib/` 넷은 **로직 그대로**, 화면 780줄만 다시 씀. ⭐ 07-31 모듈 분리의 값이 **두 번째로** 청구됐다 |
| ~~-~~ | ~~데이터 이사 (localStorage origin 이동)~~ | ✅ 완료 (2026-08-17). 드라이브 동기화가 **이사 수단**으로 그대로 쓰였다 — 저장 위치만 바뀌는 계약이라서. ⚠️ **옛 사이트를 이제 열면 안 된다** — 거기서 `올리기`를 누르면 낡은 데이터가 드라이브를 덮는다. 며칠 뒤 정리: `ALLOWED_ORIGINS`·OAuth 원본에서 옛 주소 제거 + `gh-pages` 삭제 |
| ~~1~~ | ~~**실제 319개 선별**~~ (기능이 아니라 **쓰는 일**) | ✅ 완료 (2026-08-20). ⭐ **값이 실제로 청구됐다** — 훑는 도중에 개선안 2번이 «분리»에서 «구독은 아예 저장 안 함»으로 뒤집혔다(위 절). 안 골랐으면 두 섹션짜리 화면을 만들고 끝냈을 일이다. ⭐ 그 외에 **걸린 불편은 없었다**(2026-08-20 확인) — 도구가 이미 충분했다는 뜻이고, 동시에 **1-b(죽은 채널 표시)는 여기서 근거를 얻지 못했다** → 신호등은 그대로 «실제로 죽은 채널이 눈에 띌 때» |
| ~~2~~ | ~~구독 / 내가 추가한 채널 분리 표시~~ | ✅ 완료 (2026-08-17, `ca7ff45`). **분리가 아니라 "구독을 저장 대상에서 뺐다".** 「📥 구독 목록에서 고르기」 모달(`<dialog>`) + 이름 검색 + 다중 선택. `subsCache`는 `channelCache`와 같은 지위(=`exportAll`에서 빠짐). 실측 payload 2.8KB. ⭐ 곁가지로 `active`가 뜻 하나만 갖게 됐다 |
| — | 안 고른 구독 **정리** (일회성) | 채널 화면의 「N개 정리하기」 배너. 한 번 누르면 조건이 스스로 사라진다. ⚠️ 누른 뒤 **`☁️ 올리기`까지 해야** 드라이브에도 반영된다 |
| **2** | **채널당 3개 + 「더보기」로 6개** | 2.5단계 ②의 남은 내용물. **1번이 닫혀 전제가 풀렸다(2026-08-20)** — 이제 몇 개가 맞는지 볼 수 있다. ⚠️ 07-30에 3→6으로 늘린 결정을 되돌리는 것이라 **그때 이유(본 영상이 흐려져 3개론 볼 게 없다)를 먼저 해소**해야 한다. `LIMIT`은 그대로 두고 화면 표시 개수만 바꾼다 |
| 3 | 화면 내용물 — 주제 드롭다운 · **내 재생목록 골라 쓰기** | 1번까지 되면 각각 놓일 자리가 생긴다. 재생목록은 `fetchSubscriptions`가 그대로 본 |
| 4 | 컴포넌트 + `styleguide.html` | 화면이 넷으로 갈린 뒤라야 공통 조각이 뭔지 보인다 |
| 5 | 유튜브 검색으로 채널 찾기 | ⚠️ `search.list` = **100 units/회** (하루 100번). 쿼터 설계 먼저. 그 전에 "내 구독에서 찾기"(0 units)가 수요의 대부분을 먹는다 |
| — | `@핸들 → channel_id`를 `channels.list?forHandle`로 | Worker의 최대 약점(HTML 긁기) 제거. ⚠️ **"순서 무관"이 아니었다**(2026-08-17 재검토): ⓐ `forHandle`은 **@핸들만** 찾는다 — `/c/…`·`/user/…` 옛 커스텀 URL은 API로 조회할 길이 없다 → **교체가 아니라 앞에 세우고 긁기는 폴백으로 남긴다.** ⓑ **API 키를 어디 둘지**를 정해야 한다(07-31에 Worker에서 지운 그것). 1 unit이라 공용 키로 충분하지만, **백엔드가 생기면 키는 거기 있는 게 자연스럽다** → 개선안 3번과 같이 볼 것 |
| ~~—~~ | ~~홈 화면 아이콘(PWA 최소 조각)~~ | ✅ 완료 (2026-08-05). 함정은 **iOS가 manifest를 무시하고 `apple-touch-icon`만 본다**는 것 — 원본 SVG가 둘로 갈린 이유 |
| ~~—~~ | ~~**RSS 에러 계약 정리** (1단계)~~ | ✅ 완료 (2026-08-16). 502 한 덩어리였던 것을 **200+`state`(`gone`·`empty`) / 404(오타) / 502(진짜 장애)**로 갈랐다. ⭐ "죽었다"는 고장이 아니라 **릴레이가 알아낸 사실**이라 200이다. 정상 응답은 안 바뀌어서(state 없음 = 정상) **배포 순서가 자유롭다.** ⚠️ **Worker를 대시보드에 붙여넣어야 라이브 반영** |
| ~~—~~ | ~~**RSS 404 오진 수정**~~ | ✅ 완료 (2026-08-21, `293e70b`). 살아있는 채널 **40개가 전부** "없어졌어"로 떴다. 원인은 08-16 계약의 **전제**였다 — 유튜브 RSS는 일시 장애에도 404를 준다(20번 쏴서 200이 0~3번). 본문도 **1613바이트 중 URL 한 줄만** 달라 응답만으로는 못 가른다. → **404는 증거이지 증명이 아니다.** ⭐ 고침은 재시도가 아니라 **다른 엔드포인트에 묻기**(`confirmGone` — 채널 페이지. 그날 RSS 0/20인 동안 10/10 멀쩡). 판정에 **「모른다」**가 생겼다(502). ⚠️ `gone`에 `verified` 추가 — 없으면 화면이 안 믿고 캐시를 지킨다(옛 Worker 대비). ⚠️ 응답 캐시를 **`no-store`로 걷어냈다** — `max-age=600`이 오진을 10분 굳혀 **F5로도 안 풀렸다** |
| **1-b** | 죽은 채널 **표시**(2단계) + **검사 버튼**(3단계) | 계약은 갈라졌지만 아직 **관리 화면엔 안 보인다.** 2단계 = `channelCache`에 `state` 실어 목록에 표식(⚠️ `myChannels`가 아니다 — 거긴 드라이브로 실려 나가 낡은 판정이 굳는다). 3단계 = 「죽은 채널 검사」 버튼. **자동으로 돌리면 안 된다** — 관리 화면 여는 것만으로 수백 개 요청이 나간다 |
| — | ⚠️ **1-b의 전제가 08-21에 바뀌었다** | 「죽은 채널 검사」의 **1회 비용이 늘었다** — `gone` 후보마다 채널 페이지 확인이 한 번 더 붙는다(최대 2배). 「자동으로 돌리면 안 된다」는 더 강해졌고, **확인된 `gone`만 표식·삭제 제안**으로 이어져야 한다 |
| — | **모바일에서 유튜브 앱으로 열기** | 지금은 브라우저로 열린다. ⭐ **원래는 이미 앱이 열려야 정상이다** — 유튜브가 두 OS 모두에 앱 링크(Universal Links / App Links)를 등록해뒀으므로 평범한 `https://youtube.com/watch?v=` 링크면 앱이 잡는다. 그래서 **진단이 먼저**: ⓐ `target="_blank"`가 iOS에서 Universal Link 처리를 건너뛰게 하는가(제거 실험이 제일 싸다 — `markWatched`는 동기라 같은 탭에서 나가도 기록이 안 날아간다) ⓑ 홈 화면 앱(standalone)으로 열면 외부 링크가 **인앱 브라우저**로 뜬다 ⓒ 사용자가 "Safari에서 열기"를 한 번 누르면 iOS가 기억한다(코드로 못 이김). 그래도 안 되면 **Android만** `intent://…#Intent;package=com.google.android.youtube;S.browser_fallback_url=…;end` (앱 없으면 웹으로 자동 폴백). ⚠️ `intent://`는 Chrome·삼성인터넷만 이해한다 — Firefox Android는 실패하므로 무조건 쓰면 안 된다. ⛔ **iOS는 강제 불가** — `youtube://` 스킴은 앱이 없으면 막다른 길이고 `browser_fallback_url` 같은 안전장치가 없다 |
| — | 드라이브 동기화 자동화 (국면 B 4단계) | 지금은 손으로 누르는 버튼 둘. **아플 때만** — 누르는 게 귀찮아지는 순간이 신호등 |
| ~~—~~ | ~~삭제 전파(tombstone) · 선별 전파~~ | ✅ 해소 (2026-08-15, `ee2a4c1`) — **tombstone 없이** 풀었다. 합치기를 버리고 동기화를 **대칭 덮어쓰기**로 바꾸니 삭제·선별이 그냥 따라간다. ⭐ 정교한 해법(필드별 시각)을 준비하다가, **규칙을 단순하게 바꾸니 문제 자체가 없어졌다** |
| — | 어느 쪽이 더 새 것인지 판단 (`updatedAt`) | 지금은 **마지막에 누른 쪽이 이긴다** — 앱은 최신을 모른다. 두 기기에서 번갈아 고치다 한쪽 작업을 날려먹으면 그때가 신호등 |
| **3-b** | **의견 남기기 탭** (`#/feedback`) | 🚦 **신호등 켜짐 (2026-08-17)** — 테스터 4명이 카톡으로 옮겨 전달하는 걸 불편해한다. 3번 백엔드의 **첫 손님**으로 얹는다(삽입 한 방향·충돌 없음·익명 → 가장 싼 리허설). 상세는 위 전용 절. ⚠️ 이걸 하려고 3번을 미루면 서버를 두 번 만든다 |
| 🎯 | **모든 데이터 변경을 서버에 반영** = 개선안 **5번** | 「올리기」를 눌러야 안전해지는 구조를 없앤다. **2026-08-20에 «행 단위(진짜 DB)»로 확정** — 통짜 파일은 상호작용 단위 저장이 안 된다. 벤더 판단도 3번에서 이미 났다(Neon). 상세는 위 «🗄️ 스키마 + 상호작용 단위 저장» 절 |

> 지금 localStorage에 든 것: 내 채널 · 본 영상 기록 · **나중에 볼 풀(516개)** · 재생목록 ID
> → 2026-08-07에 `💾 백업`을 지우고 **`☁️ 드라이브` 하나로 갔다.** 사본이 하나뿐이고
> `appDataFolder`는 사람이 열어볼 수 없다 — 구글 계정이 잠기면 나가는 길이 없다는 뜻이다.

그 외 대기: 통합 타임라인+주제 필터(→ 2.5단계 이후 ② 피드 화면의 문제로 내려감), 썸네일, 배치 요청

> **미정 · "DB 연동"** — 기기 동기화는 Drive로 끝났으므로(2026-08-04) "DB를 붙인다"가
> 무엇을 뜻하는지는 아직 정하지 않았다.
> 신호등: **Drive 파일 한 장으로 못 하는 일이 생겼을 때** — 나 아닌 사람과 **공유** /
> 전체를 **검색·집계** / 기기 간 **즉시** 반영. 셋 다 아니면 Drive로 충분하다.
